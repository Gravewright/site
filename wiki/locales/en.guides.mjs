// English wiki content. Edit translations here; the HTML generator contains no prose.
import { reviewEnglishGuides } from "./en.guide-review.mjs";

const sourceGuides = [
  {
    "slug": "inicio",
    "section": "Start here",
    "title": "Welcome to Gravewright",
    "summary": "Understand what Gravewright is and choose your next step.",
    "body": "# Welcome to Gravewright\n\nGravewright is a virtual table for playing RPGs in the browser. One person runs the application and assumes the role of **GM**; the rest join as players via a link or invitation code.\n\nYou don't need to know programming to play. The normal flow is:\n\n1. install and start Gravewright;\n2. create a table;\n3. choose a ruleset or use PDF sheets;\n4. prepare a scene and characters;\n5. invite the group;\n6. open the table and play.\n\n## Choose your journey\n\n- **I want to master:** go to *Installation* and then *Your first table*.\n- **I'm a player:** read *Logging in as a player* and *Getting to know the interface*.\n- **I want to publish a server:** read *Server and production* after testing locally.\n- **I want to create a system:** start in *Packages and rulesets*.\n\n## Essential concepts\n\n**Table** is the shared campaign. **Scene** is the current map. **Actor** represents character, creature or NPC. **Item** represents equipment, skill or resource defined by the ruleset. **Diary** stores texts, missions and mission boards. **Ruleset** teaches Gravewright the rules and tokens of a game.\n\n> [!NOTE]\n> Gravewright Beta is still evolving. Make backups before updates and before major campaign changes.\n\n## What you should see when you're done\n\nAfter the initial guide, you'll have a table open in your browser, a GM account, and a clear path to preparing your first session."
  },
  {
    "slug": "instalacao",
    "section": "Start here",
    "title": "Installation and first start",
    "summary": "Install ZIP desktop on Windows or checkout the source code.",
    "body": "# Installation and first start\n\n## Windows: recommended desktop package\n\nThis is the path for those who downloaded **Gravewright-<version>-win64.zip**. It already contains Python and dependencies: do not install Python, Git, or `uv`.\n\n1. In File Explorer, right-click the ZIP and choose **Extract All**.\n2. Move the extracted folder to a permanent location. Do not run the program inside the ZIP.\n3. Open the **Gravewright** folder and run **Gravewright.exe**. The file needs to remain next to the **_internal** folder.\n4. If the SmartScreen appears, confirm that the file came from the official release and use **More info → Run anyway**.\n5. In the **Gravewright Launcher** window, click **Start Gravewright**.\n6. Wait for the state to change from **Server stopped** to **Running at** followed by a local address such as `http://127.0.0.1:54321/`. The port is chosen automatically and may not be 8000.\n7. The browser opens by itself. If it doesn't open, click **Open in browser**.\n\nKeep the launcher open during the session. **Stop Gravewright** stops the server; closing the launcher also closes it.\n\n### Where the data lives\n\nWhen first started, the **GravewrightData** folder is created next to the executable. It contains SQLite database, uploads and installed packages. The **Open data folder** button opens exactly that folder. To update the app, replace the program folder, but preserve **GravewrightData**.\n\n### Maintenance via launcher\n\n- **Doctor** checks database, directories and packages; the result appears in the logs panel.\n- **Backup** creates a verified ZIP with database, assets and packages.\n- **Restore** only works when the server is stopped and asks for confirmation before replacing data.\n- **Packages** executes list, install, enable, disable, update, doctor and remove by ID.\n- **Logs** shows or hides the server log.\n\nIf the normal launcher does not start, run **Gravewright-debug.exe** in the same folder and copy the error displayed in the console.\n\n## Windows: source code checkout\n\nUse this path only if you downloaded or cloned the repository and see files like **install-windows.bat**, **pyproject.toml** and **uv.lock**.\n\n1. Extract or clone the entire repository.\n2. Double-click **install-windows.bat**.\n3. The script installs `uv` for the current user, runs `uv sync --frozen`, creates the local configuration and a `SESSION_SECRET`, runs the diagnostics, and starts the server.\n4. Wait for the **Starting Gravewright** message; the browser will open to `http://127.0.0.1:8000`.\n5. Keep the terminal window open. Closing it or pressing `Ctrl+C` shuts down the server.\n\nNext time, double-click on **install-windows.bat**. It reuses the already prepared environment.\n\nFor manual maintenance, open PowerShell at the project root:\n\n```powershell\n.\\grave.bat doctor\n.\\grave.bat run --open\n```\n\n## First access\n\n1. On the **Login** screen, click **Create account**.\n2. Fill in your name, email and a password with at least 8 characters.\n3. The first registered account receives the global role **OWNER**; This is different from being a GM of a campaign.\n4. After logging in, confirm that the **Campaigns**, **Rule Systems** and **Add-ons** tabs appear. **Settings** and **Privacy** appear only for the owner.\n\n## Diagnosis by symptom\n\n**The desktop does not find the data:** do not just move the EXE; move the full Gravewright folder and keep **GravewrightData** next to it.\n\n**Start button fails:** click **Doctor**, open **Logs** and, if necessary, use **Gravewright-debug.exe**.**The checkout informs that uv does not exist:** close the window, open it again and run **install-windows.bat**; the first installation may require a new session to update the PATH.\n\n**The page does not open:** on the desktop use **Open in browser** and the address/port shown in the launcher. At checkout use `http://127.0.0.1:8000`.\n\n**The screen appears without styles:** you opened HTML through Explorer. Gravewright must be accessed via the HTTP address opened by the launcher."
  },
  {
    "slug": "primeira-mesa",
    "section": "Start here",
    "title": "Your first table",
    "summary": "Create a campaign, choose the system and enter the game space.",
    "body": "# Your first table\n\n## Create the campaign\n\n1. Log in to your account and stay in the **Campaigns** tab on the internal panel.\n2. Click **Create campaign**.\n3. Fill in **Title** (required, up to 120 characters) and **Description** (optional).\n4. Click **Create Campaign** again. The new card appears in the list.\n5. On the card, click **Open** to join the table or **Manage table** for system, packages, members, codes and snapshots.\n\n## Choose the system\n\nIf the card shows **No Rules System Attached**, do one of the following:\n\n1. open **Rule Systems** in the internal panel;\n2. install and **Activate** an available ruleset, or use **Upload a ruleset** for a valid ZIP;\n3. in **Assign to campaign**, choose the campaign;\n4. Go back to **Campaigns** and confirm **Rule system: <name>**.\n\nIt is also possible to open the table without a system: the **Link system of rules** modal will explain that actors and items will only be released after the link. The system determines actor/item types, tokens, rolls and combat. For fillable forms, use **Gravewright PDF System**.\n\n## Minimum preparation for a test\n\nWhen opening the table as a GM, use **GM Getting Started**. The checklist follows four real actions:\n\n1. link a system;\n2. create a character through the **Actors** directory;\n3. create a scene through the **GM → Scenes** panel;\n4. generate a code through the **GM → Players** panel.\n\nTo test the board, send a map in **Scenes → Send Map**, check **Activate after sending** and wait for the *Sending*, *Preparing*, *Slicing tiles* and *Recording blocks* steps. Then drag the actor from the directory to the map to create the token.\n\nOpen a second incognito window with a gamer account. This immediately reveals differences in permissions and visibility.\n\n## Checklist before inviting the group\n\n- the correct scene is active;\n- the map opens without error;\n- the player only sees what he should;\n- the actor belongs to the player;\n- chips and items open;\n- chat sends messages and scrolls;\n- there is a recent backup.\n\n> [!TIP]\n> Prepare a test room first. Once the flow is comfortable, create or import the final campaign."
  },
  {
    "slug": "jogador",
    "section": "Players",
    "title": "Login as a player",
    "summary": "Accept an invitation, join the table, and set up your experience.",
    "body": "# Logging in as a player\n\n## Join by invitation\n\nThere are two different flows:\n\n- **Join code:** in the **Campaigns** panel, click **Join a table**, enter the code in the format shown and press **Join table**. You enter as a Player.\n- **Link with code:** log in, open the link and confirm with **Continue and enter**.\n- **Nominal invitation:** appears in **Pending invitations**. Check who invited and the role offered, then use **Accept** or **Decline**.\n\nAfter confirmation, the campaign card appears in **Campaigns** with its function. Click **Open**.\n\n## First entry\n\nThe first time at each table, Gravewright opens **Interface Mode**. Before closing, choose:\n\n- **Classic Mode:** traditional panels and direct access to tools;\n- **Gravewright Mode:** reorganized presentation for the Gravewright experience;\n- **Classic view:** less visual effects;\n- **Cinematic vision:** richer effects and presentation.\n\n- the color in **Color of your ping**;\n- **Classic Mode** or **Gravewright Mode**;\n- **Classic** or **Cinematic** vision;\n- whether **table shaders** are turned on.\n\nThese choices are saved for your user in that campaign. Changing them does not change other players' screens; later you can reopen the configuration via the gear icon.\n\n## Before the session\n\n1. confirm that your name appears online;\n2. open the actor granted by the GM;\n3. test the plug and roll;\n4. Confirm that the map and token are visible;\n5. inform the GM if any expected journal or item does not appear.\n\n## If you lose access\n\nRefresh the page and return to the table list. If the campaign disappeared, the invitation may have been revoked or your membership removed. A banned player is immediately kicked and cannot reuse the old invite."
  },
  {
    "slug": "interface",
    "section": "Players",
    "title": "Getting to know the interface",
    "summary": "Learn to navigate between map, directories, tools and windows.",
    "body": "# Knowing the interface\n\nThe center of the screen is the board. The directories are on the side and open scenes, actors, items, diaries, combat and settings.\n\n## Windows\n\nTabs, permissions, creation and settings open in windows. The top bar offers drag, minimize, maximize/full screen, and close. The **New Actor**, **New Item**, **Create Entry** and **Permissions** windows open next to the side tab that originated them.\n\n## Map navigation\n\n- use the mouse wheel to zoom in and out;\n- drag the board with the appropriate tool;\n- select tokens with click;\n- use ping to indicate a point to the group;\n- measurements help check distances.\n\n## Directories and tools\n\nIn the right column are Search, Chat, Actors, Items, Diary, Combat and settings. On the table are the tools with shortcuts: **Select (S)**, **Target (T)**, **HP (H)**, **Ruler (R)**, **Drawing (D)**, **Notes (N)**, **Markers (M)**, **Walls (W)**, **Mist (F)** and **Lighting (L)**.\n\nFolders only organize actors, items and journals; do not grant access. Right-click an entry to **Open**, **Permissions** or delete. To move content, drag the entry to the folder and check the folder counter.\n\n## Safe shortcuts to learn\n\nHover your cursor over the buttons before clicking. Tool names appear in tooltip. Test moving, zooming, selecting, and opening cards before using wall or delete tools.\n\n## Recovery\n\nIf a window becomes out of position, close it and open it again. If the interface stops responding, reload the page; changes persisted on the server remain saved."
  },
  {
    "slug": "jogadores-permissoes",
    "section": "Master's student",
    "title": "Players, invitations and permissions",
    "summary": "Invite the group and control reading, ownership and access.",
    "body": "# Players, invitations and permissions\n\n## Invite\n\nInside the table, open **GM → Players** and use **Generate Entry Code**. Copy the code or link displayed and send it to the group. Codes place the person as a Player; for another function, use the nominal invitation available in campaign management.\n\n## Campaign roles\n\n- **GM:** manages campaign, members, scenes and all content;\n- **Assistant GM:** delegated administrative function, without turning the user into global owner;\n- **Player:** uses the board and only resources granted;\n- **Streamer:** reading session generated by link, without chat or table controls.\n\n## Permissions for actors, items and journals\n\nIn the **Actors**, **Items**, or **Journal** directories, right-click the entry and choose **Permissions**. The modal follows the same model in all three resources. For **Players**, select:\n\n- **None:** the resource is not displayed;\n- **Read only:** can open, but not change;\n- **Owner:** can edit within the limits of the ruleset.\n\nFinish with **Save permissions**. Use **Owner** for the character sheet; **Read-only** for visible NPCs, reference items, and handouts. Folders do not automatically propagate permissions.\n\n## Remove or ban\n\nRemove ends the current membership. Banning also prevents the reuse of previously accepted invitations and immediately kicks the connected person out.\n\n> [!WARNING]\n> Test permissions with a real player account. The GM's view shows content that might be correctly hidden from the group."
  },
  {
    "slug": "cenas",
    "section": "Master's student",
    "title": "Scenes, maps, walls and vision",
    "summary": "Create a playable map and control what each character sees.",
    "body": "# Scenes, maps, walls and vision\n\n## Create a scene\n\n1. open **GM → Scenes**;\n2. in **Send map**, fill in **Scene name** and choose **Map image**;\n3. choose a **Group** or leave **No group**;\n4. set **Visibility**: Players, GM Only or Hidden;\n5. configure **Show grid**, color, opacity and **Grid size**. The grid is independent of the image resolution;\n6. keep the tile raster and chunk at defaults if you don't know why to change them;\n7. Check **Activate after submit** and submit.\n\nWait for the sequence **Sending file → Preparing map → Slicing tiles → Saving blocks → Done**. Closing during this process may stop creation.\n\nThen the scene card offers **Navigate without loading to table**, **Activate** and **Configure**. Browsing only changes your view; activating changes the shared scene. In **Configure**, adjust the scene or use **Use current view as start**.\n\n## Walls and doors\n\nPress **W** or choose **Walls** and select **Wall**, **Door**, **Window** or **Secret Passage** before drawing. Click on the contour points and connect the ends; gaps allow vision. Return to **Select (S)** to edit or delete segments. Test doors as GM and then in **Toggle GM/player view**.\n\n## Dynamic lighting and vision\n\nSelect the token and open **Token View** to configure range and who can see through it. Press **L** to add light and adjust range, color and intensity in the editor. The player needs to own the actor/token to use its vision. Use **Toggle GM/player view**; Just looking at it as GM does not validate the result.\n\n## Mist and revelation\n\nFog of war controls known areas. Reveal only what is necessary and use reset with caution, as it changes the shared state.\n\n## Diagnosis\n\nIf a player sees through a wall, look for disconnected edges. If everything is dark, confirm token vision, ambient light and actor ownership. If the map is slow, reduce effects and try classic mode."
  },
  {
    "slug": "atores-tokens",
    "section": "Master's student",
    "title": "Actors and tokens",
    "summary": "Create characters, grant ownership, and place them on the map.",
    "body": "# Actors and tokens\n\n## Create an actor\n\n1. open the **Actors** tab on the right side;\n2. click on **Create Actor**;\n3. enter the name and choose a type provided by the ruleset;\n4. confirm; the entry appears in the directory;\n5. click to open the form and fill in the system fields.\n\nIf there are no types or creation says the system is not activated, go back to the dashboard, activate a ruleset and link it to the campaign.\n\n## Give the character to the player\n\nRight click on the actor, choose **Permissions**, mark the player as **Owner** and save. **Read only** allows you to consult, but not control as a character. Then log in with the player's account and confirm that the form opens and saves.\n\n## Put on map\n\nWith a scene active, drag the sprite input to a point on the map. This creates a token instance linked to the actor. Dragging again creates another instance; does not duplicate the directory actor.\n\n## Configure the token\n\nSelect the token and review image, size, name, arrangement, bars and view. The slashes come from the ruleset mappings. If HP does not appear, confirm that the chip has the expected value and that the ruleset declared the mapping. The **HP (H)** tool applies Damage, Heals, or Sets value to the controlled token; GMs control everyone.\n\n## Movement\n\nPlayers only move authorized tokens. Walls and doors can limit the path. The GM can reposition and fix locked tokens.\n\n## Good practices\n\nUse folders to separate characters, allies, and creatures. Give unique names and keep a moderately sized image for each actor."
  },
  {
    "slug": "itens-fichas-pdf",
    "section": "Master's student",
    "title": "Items, sheets and PDFs",
    "summary": "Use ruleset sheets, equipment and fillable PDF documents.",
    "body": "# Items, files and PDFs\n\n## Items\n\n1. open **Items** and click **Create Item**;\n2. enter the name and choose a type provided by the ruleset;\n3. open the item and fill in your form;\n4. to share the dropped item, use right button → **Permissions**;\n5. to incorporate it into an actor, drag it to an area of ​​the card that accepts that type.\n\nIf the drop is rejected, the zone may not accept the type, the item may belong to another ruleset, or the system may not have a drop action defined. Creating a folder does not change any of these behaviors.\n\n## HTML and declarative tokens\n\nThe ruleset controls the available fields and actions. Changes are saved by the server; wait for the save indicator before closing.\n\n## Gravewright PDF System\n\nUse this ruleset when the game has a fillable PDF sheet:\n\n1. in the internal panel, open **Rule Systems**;\n2. locate **Gravewright PDF System**, use **Install** if it appears as Available and then **Activate**;\n3. in **Assign to campaign**, select your table;\n4. open the campaign and create an actor of the type provided by the package;\n5. open the form and load the PDF using the control of the form itself;\n6. change some fields, close and reopen the actor to test persistence;\n7. drag the actor to the scene and check the name/bars defined by the package mapping.\n\nThe submitted PDF and form values ​​are stored by Gravewright; opening the file outside the application does not overwrite the saved data. Only actual AcroForm fields are editable. A scanned or \"flattened\" PDF can be viewed, but does not offer fields.\n\n## Security and copyright\n\nOnly send PDFs that you have the right to use. User-submitted PDFs remain in campaign storage and should not be added to the core repository.\n\n## When a field doesn't work\n\nConfirm that the PDF actually has form fields. Scan-only PDFs do not offer editable fields. Also check the ruleset mappings and the diagnostics console."
  },
  {
    "slug": "diarios-missoes",
    "section": "Master's student",
    "title": "Diaries, missions and boards",
    "summary": "Organize campaign texts and publish missions for players.",
    "body": "# Diaries, missions and boards\n\n## Diaries\n\nOpen the **Journal** tab and use **Create Entry**. Enter the title, choose **Journal** under **Type** and click **Create Journal**. Inside the editor:\n\n- type `/` to insert Text, Titles, lists, quote, separator, image, **GM Note** or **Secret**;\n- use **New section** for Page, Chapter, Section or Subsection;\n- in **Audience**, choose **Table** or **Master only** per section;\n- track the status **Changed → Saving… → Saved** before closing;\n- use right click → **Permissions** on the directory to set None, Read only or Owner.\n\n**Create Folder** and subfolders organize entries but do not make their contents public.\n\n## Create a mission\n\n1. in the journals directory, choose **Create Entry**;\n2. select **Mission**;\n3. enter the title;\n4. open the mission and click **Edit**;\n5. fill in location, contractor, summary, public description and tags;\n6. add goals and rewards;\n7. only mark the elements that should be visible;\n8. change the status to **Available**.\n\nIn editing, the side navigation separates **Content**, **Objectives**, **Rewards** and **GM-exclusive area**. On each objective or reward, mark **visible** only when the player can see it. Objectives can also be marked as optional and completed.\n\nDraft or archived missions do not appear in the public record or on the board.\n\n## Mission board\n\nCreate an entry of type **Mission Board** and open **Edit**. Under **Linked Missions**, choose a mission and use **Add Mission**. For each line you can change the status, pin/unpin, sort by arrows or remove from the board. The status filter in this same area reduces the administrative list; it does not change the status alone.\n\nIn public preview, only linked missions that are not in **Draft** or **Archived** generate cards. If none are publishable, the empty wall is centered.\n\n## Status\n\nUse **Available**, **In Progress**, **Completed** or **Failed** depending on the narrative. The change is transmitted to connected players.\n\n## Visibility\n\nHidden objectives and rewards remain available to the GM, but do not appear on the players' records. This allows you to reveal consequences and goals at the right time."
  },
  {
    "slug": "chat-combate",
    "section": "Playing",
    "title": "Chat, data and combat",
    "summary": "Conduct rolls, initiative, turns, and communication during the session.",
    "body": "# Chat, data and combat\n\n## Chat\n\nOpen **Chat**, write in the **Message... field (/ to see commands)** and send. Available commands include:\n\n- `/roll 2d6+3` for a public scroll;\n- `/gmroll 1d20` for results visible only to you and the GM;\n- `/whisper nome mensagem` for another player;\n- `/gm mensagem` to talk to the GM;\n- `/me ação` for third person emote.\n\nThe GM can delete a message or **Delete all messages**. Scrolls and chat are validated by the server; editing local HTML does not change results.\n\n## Scrolls\n\nIn the **Data Tray**, click the data or write a **Formula**, for example `2d20L1+3`. Adjust quantity, modifier, discard minors/majors, explosion and extra die when applicable. Fill in **Roll Name** and choose **Roll** or **To GM**. Formulas that the server does not recognize are rejected.\n\n## Start combat\n\n1. with **Select (S)**, select the tokens involved;\n2. open **Combat** and click **Add selected tokens**;\n3. use **Initiative Roll**, **NPC Roll**, **Roll Missing** or set an individual value;\n4. click on **Start combat**;\n5. advance to **Next turn** or **Next round**;\n6. in the combatant's menu, center token, open token, pass turn, hide/reveal or mark as defeated.\n\nThe ruleset can change initiative, tiebreakers and card presentation.\n\n## During shift\n\nUpdate conditions and resources via the form or actions offered. Confirm that the active combatant matches the highlight on the tracker.\n\n## Close\n\nUse **End Combat** only when the encounter has ended. This clears the active state; Important narrative records should be noted in the diary."
  },
  {
    "slug": "cartas-efeitos",
    "section": "Playing",
    "title": "Cards, lights and effects",
    "summary": "Use decks and visual atmosphere without compromising the session.",
    "body": "# Cards, lights and effects\n\n## Letters\n\nLetters only appear when the active ruleset or add-on declares this feature. In this case, open the panel provided by the package to create/use decks, buy, reveal and discard. If there is no tab or card action, there is no generic deck to configure in core: first activate a package that offers the interface.\n\nBefore the session, test purchase, discard, and visibility with a player account.\n\n## Lights\n\nPress **L**, choose the lighting tool, click on the scene and configure the light in the editor. Use **Game Layer** for something visible on the table and **GM Layer** for hidden setup. Then select **Toggle GM/Player Vision** to validate walls, range and the token that provides vision.\n\n## Particles and shaders\n\nThe GM edits particles and shaders through the scene controls. Each effect has its own configuration and is transmitted to the table. The **Table Shaders** personal button, in **Interface Mode**, allows the player to turn off shaders locally without erasing the GM configuration.\n\n## Performance rule of thumb\n\nIf the table loses fluidity:\n\n1. disable shaders;\n2. reduce particles;\n3. Dim animated lights;\n4. test in classic mode;\n5. Check the size of the map and images.\n\nAtmosphere should support the session, not impede interaction with tokens and tokens."
  },
  {
    "slug": "administracao",
    "section": "Administration",
    "title": "Campaigns, snapshots and backups",
    "summary": "Protect table data and recover unwanted changes.",
    "body": "# Campaigns, snapshots and backups\n\n## Snapshot and backup are not the same thing\n\n**Snapshot** belongs to a campaign and is in the same bank. It includes actors, items, journals, scenes, tokens, combat, chat, permissions, and campaign files; does not include global accounts, sessions, auditing, or snapshot history. **Backup** is an external ZIP of the SQLite installation and may include assets and packages.\n\n## Recommended routine\n\nIn the **Campaigns** dashboard, open **Manage table → Snapshots**. Enter name and description and click **Create snapshot**. To go back, choose **Restore this version**; Gravewright first creates an automatic security snapshot. **Delete** only removes that point, not the campaign.\n\nUse snapshots before importing content or changing the campaign. Use backup before upgrading Gravewright or changing packages and store the ZIP outside of the installation.\n\n## Via CLI\n\n```powershell\n.\\grave.bat doctor\n.\\grave.bat db status\n.\\grave.bat backup -o gravewright-backup.zip --include-assets --include-packages --verify\n```\n\nOn the desktop, **Backup** already includes assets, packages and verification. To restore, stop the server, click **Restore**, choose the ZIP and confirm. At checkout, test first with `.\\grave.bat restore arquivo.zip --dry-run` and only then use `--yes`.\n\n## Clone and export\n\nCloning is useful for testing changes without affecting the original table. Export transfers a campaign according to the enabled resources. Always check permissions and required packages after importing.\n\n## Before updating\n\nStop new edits, take a verified backup, note the current version, and run the migration. Then test login, campaign opening, scenes, tokens and sent files."
  },
  {
    "slug": "servidor",
    "section": "Administration",
    "title": "Server, production and security",
    "summary": "Publish a table with HTTPS, own secrets and protected database.",
    "body": "# Server, production and security\n\nStart locally. Publish only after GM and player can complete a test session.\n\n## Production requirements\n\n- domain and HTTPS;\n- long and unique `SESSION_SECRET`;\n- secure cookies;\n- allowed WebSocket hosts and sources;\n- PostgreSQL for persistent multi-user installations;\n- backup storage;\n- reverse proxy configured for WebSocket.\n\nThe desktop launcher listens only in loopback and is for local use. It does not publish the table for external players. For internet, run the server via Docker or managed process behind HTTPS proxy.\n\n## Variables\n\nStart from `.env.production`, but don't publish secrets to Git. Configure `APP_ENV=production`, `DATABASE_URL`, `SESSION_SECRET`, `ALLOWED_HOSTS`, `WS_ALLOWED_ORIGINS` and secure cookies. The proxy needs to forward WebSocket upgrade to `/game/ws`.\n\n## Publishing checklist\n\n1. perform migrations;\n2. execute `grave doctor`;\n3. access via HTTPS;\n4. test login on another device;\n5. Confirm connection in real time;\n6. send and retrieve a file;\n7. test backup and restore;\n8. Monitor logs without exposing personal data.\n\n> [!WARNING]\n> Do not directly expose a development server to the internet. Use HTTPS proxy, firewall and own secrets.\n\nSee `SECURITY.md` to report vulnerabilities responsibly."
  },
  {
    "slug": "solucao-problemas",
    "section": "Administration",
    "title": "Diagnosis and troubleshooting",
    "summary": "Investigate failures without putting campaigns at risk.",
    "body": "# Diagnosis and troubleshooting\n\n## Start with the doctor\n\n```powershell\n.\\grave.bat doctor\n.\\grave.bat db status\n```\n\nOn the desktop, click **Doctor** and then **Logs**. At checkout, use the commands above. Record version, operating system, time, exit code, and the first error message.\n\n## Screen does not load\n\nOn the desktop, confirm **Running at ...** and use **Open in browser**; the door is dynamic. At checkout, confirm that the terminal is still open and use `http://127.0.0.1:8000`. A page opened by `file:///` is not the application. Then test incognito window, browser console and logs.\n\n## Real time does not work\n\nIf chat, presence or updates are not arriving, check the WebSocket, proxy and HTTPS origin. The HTTPS page must use secure WebSocket.\n\n## File or map fails\n\nCheck storage directory format, size, disk space, and permissions. Do not rename internal files manually.\n\n## Late bank\n\nDo not change tables by hand. Backup and perform official migration. If the migration fails, preserve the database and logs before trying again.\n\n## When asking for help\n\nSend reproducible steps and error messages, but remove emails, tokens, secrets, private content, and campaign files."
  },
  {
    "slug": "cli",
    "section": "Creators",
    "title": "Gravewright CLI",
    "summary": "Use the command line to run, validate, and maintain the project.",
    "body": "# Gravewright CLI\n\nThe main command is `grave` on macOS/Linux and `grave.bat` on Windows.\n\n## Essential commands\n\n```text\ngrave doctor\ngrave run --open\ngrave backup -o backup.zip --verify\ngrave restore backup.zip\ngrave db status\ngrave db upgrade\ngrave package list\ngrave package validate caminho/do/pacote\ngrave package doctor id-do-pacote\n```\n\nOn Windows use the prefix `.\\grave.bat`; on macOS/Linux, `./grave`. The launchers are called `uv run python -m app.cli` and must be run in the root of the repository. Use `--help` at any level:\n\n```powershell\n.\\grave.bat --help\n.\\grave.bat package --help\n.\\grave.bat ruleset new --help\n```\n\n## Output for automation\n\nPacket commands offer `--json` when indicated. Use this output in CI instead of interpreting color tables.\n\n## Flow to validate a package\n\n1. generate the structure with `.\\grave.bat ruleset new meu-sistema --name \"Meu Sistema\" --sheets --rolls --combat --content`;\n2. validate with `.\\grave.bat package validate data/packages/rulesets/meu-sistema`;\n3. install with `.\\grave.bat package install meu-sistema --yes --enable`;\n4. run `.\\grave.bat package doctor meu-sistema`;\n5. link to a disposable campaign and test as GM and Player;\n6. generate `grave.lock.json` with `.\\grave.bat lock -o grave.lock.json` to register the installed set.\n\nA non-zero exit code means that the automation must stop."
  },
  {
    "slug": "pacotes-rulesets",
    "section": "Creators",
    "title": "Packages, SDK and rulesets",
    "summary": "Create extensions using public capabilities without depending on the internal core.",
    "body": "# Packages, SDK and rulesets\n\nAll installable content is a package. The main types are **ruleset**, **addon**, **library**, **theme**, **content** and **assets**.\n\n## Create a ruleset\n\n```powershell\n.\\grave.bat ruleset new meu-rpg --name \"Meu RPG\" --sheets --rolls --combat --content\n```\n\nThe package is created in `data/packages/rulesets/meu-rpg`. `manifest.json` identifies type, version, SDK, capabilities and declarative entries. Schemas define data; layouts define chips; rolls, combat and mappings files define authorized behavior.\n\n## Manifest\n\nOnly declare capabilities that are actually used. Provide compatibility with SDK 1, dependencies and files by relative paths. `package validate` checks schema, references and capabilities; `package doctor` also checks installation, integrity and dependencies.\n\n## Public runtime\n\nScripts access the SDK via the object delivered in `setup(sdk)`. Do not use core imports, private routes, database, filesystem or raw WebSocket. For persistence use `storage.sqlite`; for messages, assets and tokens declare the corresponding public capability. The server continues validating role, campaign and ownership.\n\n## Development cycle\n\n1. generate the package;\n2. validate via CLI;\n3. install and enable;\n4. activate in a test campaign;\n5. open as GM and player;\n6. run the doctor;\n7. Only package declared files.\n\n## Compatibility\n\nSDK 1 is the stable line. Prefer compatible additions and test min and verified versions declared in the manifest."
  },
  {
    "slug": "sdk-manifest",
    "section": "In-depth SDK",
    "title": "Manifest and cycle of a package",
    "summary": "Understand each block of the manifest, activation, validation and publication.",
    "body": "# Manifest and cycle of a package\n\n`manifest.json` is not decorative metadata: it is the contract that the core validates before loading any file. It answers **who the package is**, **where it can be activated**, **what it provides** and **which APIs it requests access to**.\n\n## Minimum package explained\n\n```json\n{\n  \"$schema\": \"https://raw.githubusercontent.com/Gravewright/gravewright/main/schemas/gravewright-package-v1.schema.json\",\n  \"schemaVersion\": 1,\n  \"sdkVersion\": \"1\",\n  \"kind\": \"addon\",\n  \"id\": \"minha-condicao\",\n  \"name\": \"Minha Condição\",\n  \"version\": \"0.1.0\",\n  \"compatibility\": { \"minimum\": \"1\", \"verified\": \"1\", \"maximum\": \"1.x\" },\n  \"capabilities\": [\"assets.scripts\", \"assets.ui\"],\n  \"activation\": { \"scope\": \"campaign\", \"mode\": \"multiple\" },\n  \"entrypoints\": { \"game\": { \"scripts\": [\"scripts/main.js\"] } },\n  \"settings\": [], \"dependencies\": [], \"conflicts\": [], \"provides\": {}\n}\n```\n\n- `schemaVersion` selects the JSON format; in SDK 1 it is number `1`.\n- `sdkVersion` selects the API line, not the commercial version of Gravewright.\n- `kind` defines the role: ruleset is exclusive base; addon and theme are extensions; library is passive dependency; content and assets deliver content.\n- `id` is permanent identity. It must match the folder and `GravewrightSDK.register({ id })`.\n- `version` should go up when you publish new assets; this also avoids stale cache.\n- `activation` controls where to load. Ruleset normally uses `exclusive`; addon, `multiple`; library, `passive`.\n- `entrypoints` tells you to load JS/CSS. Declaring a file in another block does not automatically execute it.\n\n## provides: what the core actually consumes\n\nUse `actorTypes` and `itemTypes` for types and their tokens; `rules.formulas` for rules; `mappings.tokens` for projection into the token; `contentPacks`, `assets`, `locales` and `areaMarkers` when necessary. Don't invent `provides.sheets`, `provides.rolls` or `provides.combat`: these top keys are not active canonical contracts.\n\n## Validate, install and activate\n\n```powershell\n.\\grave.bat package validate data/packages/addons/minha-condicao --json\n.\\grave.bat package install minha-condicao --yes --enable\n.\\grave.bat campaign package activate ID_DA_CAMPANHA minha-condicao\n.\\grave.bat package doctor minha-condicao\n```\n\nValidation checks the file on disk. Setup records a snapshot. Enable makes the package globally available. Activate loads you into a campaign. Confusing these steps is the most common cause of “the script exists but does not run”. When editing an installed package, use `package update`, increment the version and reload the table.\n\n## Don't\n\n- do not use absolute paths, `..` or files outside the package;\n- do not declare a capability “by guarantee”;\n- do not change `id` after publishing: this creates another package;\n- do not place inline code in HTML or manifest;\n- do not mark optional dependency as mandatory; degrade gracefully when possible."
  },
  {
    "slug": "sdk-capabilities-runtime",
    "section": "In-depth SDK",
    "title": "Capabilities and JavaScript runtime",
    "summary": "Register scripts, use authorized APIs, and handle the lifecycle correctly.",
    "body": "# Capabilities and JavaScript runtime\n\nCapability is a technical and auditable permission. The method remains subject to user permissions: declaring `actors.write` does not allow a Player to change an actor it does not control.\n\n## Complete registration\n\n```js\nwindow.GravewrightSDK.register({\n  id: \"minha-condicao\",\n  setup(sdk, { package: manifest, context }) {\n    console.log(\"configurando\", manifest.id, context.campaignId);\n  },\n  ready(sdk, { context }) {\n    sdk.ui.toast(\"Addon carregado nesta campanha\");\n  }\n});\n```\n\n`setup` runs when the active manifest is known; use it to register controllers, commands and subscriptions. `ready` runs when the table is ready; use it for actions that depend on the full runtime. The received `sdk` is already filtered for that packet.\n\nFor the example, the manifest needs `assets.scripts` to load the file and `assets.ui` for `sdk.ui.toast`. Without the second, the runtime throws a message indicating the missing package, method and capability.\n\n## Reading and writing are not the same permission\n\n`actors.read` releases `sdk.actors.list/get`; `actors.write` release create/update/delete; `actors.data.write` releases validated plug data patch. The same division exists for items. To scroll use `dice.roll`; for an intent declared in rules use `rolls.intent`.\n\n```js\nconst actors = await sdk.actors.list();\nconst hero = actors.find((actor) => actor.type === \"character\");\nif (hero) {\n  await sdk.dice.roll({ formula: \"1d20+3\", label: \"Teste de \" + hero.name });\n}\n```\n\nThis code requires `actors.read` and `dice.roll`. The server filters the list by the current user and calculates the scroll; do not use `Math.random` for a game roll.\n\n## Communication between packets\n\nPublish versioned events and subscribe to namespaced names:\n\n```js\nsdk.bus.publish(\"minha-condicao.aplicada\", { version: 1, actorId, condition: \"cego\" });\nconst unsubscribe = sdk.bus.subscribe(\"outro-pacote.atualizado\", (payload) => {\n  if (payload?.version !== 1) return;\n  console.log(payload);\n});\n```\n\nDeclare `bus.publish` and/or `bus.subscribe`. Save `unsubscribe` and recall it when dismounting. Don't use generic events like `updated`; they collide and have no owner.\n\n## Prohibited APIs\n\n`backend.execute`, `database.raw`, `filesystem.raw`, `network.raw` and `permissions.override` are rejected. Don't get around this by calling `fetch` for internal routes, accessing renderer globals, or changing the core DOM. These surfaces are not API and can break or leak data."
  },
  {
    "slug": "sdk-fichas-html",
    "section": "In-depth SDK",
    "title": "HTML files, bindings and controllers",
    "summary": "Build an editable, persistent and secure record from start to finish.",
    "body": "# HTML files, bindings and controllers\n\nAn HTML file has four parts: data schema, template, CSS and optional controller. The type connects everything in the manifest.\n\n## Type in manifest\n\n```json\n{\n  \"id\": \"character\",\n  \"label\": \"Personagem\",\n  \"schema\": \"schemas/actors/character.schema.json\",\n  \"sheet\": {\n    \"mode\": \"html\",\n    \"template\": \"sheets/character.html\",\n    \"controller\": \"scripts/character-sheet.js\",\n    \"style\": \"styles/character-sheet.css\"\n  }\n}\n```\n\nThe manifest also needs `sheets.html`, `sheets.controller`, `assets.scripts` and `assets.styles`; JS and CSS need to appear in `entrypoints.game`. `sheet.style` alone does not inject CSS, and `sheet.controller` alone does not execute JS.\n\n## Template that saves\n\n```html\n<form class=\"meu-rpg-sheet\">\n  <input data-bind=\"actor.name\" aria-label=\"Nome\">\n  <label>Força <input type=\"number\" data-bind=\"system.forca\"></label>\n  <p>Tipo: <span data-text=\"actor.type\"></span></p>\n  <button type=\"button\" data-roll=\"1d20 + @sheet.forca\" data-roll-label=\"Teste de Força\">Rolar</button>\n  <button type=\"button\" data-action=\"mostrar-resumo\">Resumo</button>\n</form>\n```\n\n`actor.*` is identity; in item use `item.*`; `system.*` is the data validated by the schema. `data-bind` reads and persists; number becomes number; other inputs normally produce string. `data-text` uses safe textContent. `data-rich-text` requires `sheets.richText` and goes through sanitizer. `data-roll` requires `dice.roll`.\n\nDo not write `system.sheet.forca` or indices/wildcards: these roots are not part of the contract. Checkbox has no special Boolean coercion; treat it in a controller if you need a real boolean.\n\n## Controller and cleaning\n\n```js\nwindow.GravewrightSDK.register({\n  id: \"meu-rpg\",\n  setup(sdk) {\n    sdk.sheets.registerController(\"character\", {\n      mount(ctx) {\n        ctx.root.classList.toggle(\"is-readonly\", !ctx.data.canEdit);\n      },\n      async onAction(action, ctx) {\n        if (action.name !== \"mostrar-resumo\") return;\n        sdk.ui.toast(ctx.actor.name + \": Força \" + ctx.data.system.forca);\n      },\n      unmount(ctx) {\n        // remova timers, observers e listeners externos aqui\n      }\n    });\n  }\n});\n```\n\nAlways search within `ctx.root`; several chips may be open. `canEdit` is for UX, not security: the server still decides. Do not replace `root.innerHTML` in the update, as it will destroy bindings. Do not use `onclick`, `<script>` in the template or global CSS selectors.\n\n## Diagnosis\n\nUnstyled HTML: CSS is not in entrypoints. Dead button: script did not load, IDs do not match or capability is missing. Field returns to the old value: schema rejected or user cannot edit. See Console and Network; assets should respond 200 in `/sdk/packages/<id>/asset/...`."
  },
  {
    "slug": "sdk-storage-settings",
    "section": "In-depth SDK",
    "title": "Storage SQLite and settings",
    "summary": "Persist first-party data without raw database and choose the right scope.",
    "body": "# Storage SQLite and settings\n\nUse **settings** for declared and small configuration. Use **storage.sqlite** for your own tables, queries, and structured state. Do not use storage to duplicate actors, items or data that already belong to the core.\n\n## Managed storage\n\nIn the manifest, declare `storage.sqlite`, scopes, migrations, queries and limit. Migrations create tables; `queries.json` contains named operations. The package never receives a database path or arbitrary SQL.\n\n```json\n{\n  \"queries\": {\n    \"getState\": { \"type\": \"read\", \"params\": { \"key\": \"string\" }, \"sql\": \"SELECT value_json FROM addon_state WHERE key=:key LIMIT 1\" },\n    \"saveState\": { \"type\": \"write\", \"params\": { \"key\": \"string\", \"value_json\": \"json-string\" }, \"sql\": \"INSERT INTO addon_state(key,value_json) VALUES(:key,:value_json) ON CONFLICT(key) DO UPDATE SET value_json=:value_json\" }\n  }\n}\n```\n\n```js\nawait sdk.storage.sqlite.execute(\"campaign\", \"saveState\", {\n  key: \"painel\", value_json: JSON.stringify({ collapsed: true })\n});\nconst rows = await sdk.storage.sqlite.query(\"campaign\", \"getState\", { key: \"painel\" });\nconst state = rows[0] ? JSON.parse(rows[0].value_json) : {};\n```\n\nMissing, extra or wrong type parameters are rejected. A named query contains a statement; ATTACH, PRAGMA, VACUUM and multiple statements are prohibited. Never assemble SQL with concatenation: declare parameters.\n\n## Settings by scope\n\n`client` stays in that browser; `user` tracks the user and is global across campaigns; `campaign` is shared across the table; `package` is general configuration. The effective precedence is default → campaign → user.\n\n```json\n\"settings\": [{\n  \"key\": \"dificuldade\", \"type\": \"enum\", \"scope\": \"campaign\",\n  \"default\": \"normal\", \"options\": [\"facil\", \"normal\", \"dificil\"],\n  \"label\": \"Dificuldade\", \"description\": \"Altera os modificadores desta mesa.\"\n}]\n```\n\n```js\nconst value = sdk.settings.get(\"dificuldade\");\nconst off = sdk.settings.onChange(\"dificuldade\", ({ value }) => atualizarUI(value));\nawait sdk.settings.set(\"dificuldade\", \"dificil\");\n```\n\nDeclare `settings`. Don't rename published keys without migration, don't save secrets in the client setting and don't use user scope for a rule that needs to be the same for everyone. Values ​​are strictly adhered to; enum out of options and invalid number generate `sdk.settings.invalid_value`."
  },
  {
    "slug": "sdk-tutorial-addon",
    "section": "In-depth SDK",
    "title": "Tutorial: functional addon from scratch",
    "summary": "Create, install, test and debug a real addon without modifying the core.",
    "body": "# Tutorial: functional addon from scratch\n\nLet's create an addon that registers the namespaced command `saudacao-mesa.mostrar`, reads a campaign setting and displays a toast. The result will only be loaded in campaigns where the package is active.\n\n## 1. Generate the structure\n\nIn the Gravewright root:\n\n```powershell\n.\\grave.bat addon new saudacao-mesa --name \"Saudação da Mesa\" --js --settings\n```\n\nConfirm the `data/packages/addons/saudacao-mesa` folder. Do not move the package to `static/` or `app/`.\n\n## 2. Manifesto\n\nUse these essential parts:\n\n```json\n{\n  \"schemaVersion\": 1,\n  \"sdkVersion\": \"1\",\n  \"kind\": \"addon\",\n  \"id\": \"saudacao-mesa\",\n  \"name\": \"Saudação da Mesa\",\n  \"version\": \"0.1.0\",\n  \"compatibility\": { \"minimum\": \"1\", \"verified\": \"1\", \"maximum\": \"1.x\" },\n  \"capabilities\": [\"assets.scripts\", \"assets.ui\", \"commands.register\", \"settings\"],\n  \"activation\": { \"scope\": \"campaign\", \"mode\": \"multiple\" },\n  \"entrypoints\": { \"game\": { \"scripts\": [\"scripts/main.js\"] } },\n  \"settings\": [{\n    \"key\": \"mensagem\", \"type\": \"string\", \"scope\": \"campaign\",\n    \"default\": \"Bem-vindos à aventura!\", \"label\": \"Mensagem de saudação\",\n    \"description\": \"Texto mostrado pelo comando /saudacao.\"\n  }],\n  \"dependencies\": [], \"conflicts\": [], \"provides\": {}\n}\n```\n\nEach method used has a capability: load JS → `assets.scripts`; register command → `commands.register`; read configuration → `settings`; toast → `assets.ui`.\n\n## 3. Script\n\nCreate `scripts/main.js`:\n\n```js\nwindow.GravewrightSDK.register({\n  id: \"saudacao-mesa\",\n  setup(sdk) {\n    sdk.commands.register(\"saudacao-mesa.mostrar\", () => {\n      const mensagem = sdk.settings.get(\"mensagem\");\n      sdk.ui.toast(mensagem || \"Bem-vindos!\");\n    });\n  }\n});\n```\n\nThe ID must be identical to the manifest. The command is registered in `setup`, as this is runtime configuration. The name receives the package namespace so as not to collide. The message is read within the handler, so subsequent changes to the setting are respected. `sdk.commands.register` registers a client command; it does not automatically create a chat slash command.\n\n## 4. Validate and upload\n\n```powershell\n.\\grave.bat package validate data/packages/addons/saudacao-mesa --json\n.\\grave.bat package install saudacao-mesa --yes --enable\n.\\grave.bat campaign package activate ID_DA_CAMPANHA saudacao-mesa\n.\\grave.bat package doctor saudacao-mesa\n```\n\nOpen or reload the campaign and confirm in the Console that registration was not declined. Trigger `saudacao-mesa.mostrar` by a host/consumer package command integration; toast should show the default. If you specifically want `/saudacao` in chat, you need to implement documented chat integration: recording a client command does not change the chat parser.\n\n## 5. Change during development\n\nAfter editing, change `version` to `0.1.1`, run `.\\grave.bat package update saudacao-mesa` and completely reload the page. Do not reinstall from above, do not edit the snapshot in the database and do not manually copy the JS to static.\n\n## 6. When it fails\n\n- package does not appear: validate type, folder and installation;\n- appears, but does not load: confirm enabled global and active in the campaign;\n- register refused: compare the two IDs;\n- command does not exist: check Network for JS and `commands.register`;\n- capability error: declare exactly the capability mentioned and update the package;\n- old code: increment version, update and reload without cache."
  },
  {
    "slug": "ruleset-pdf",
    "section": "Creators",
    "title": "Gravewright PDF System",
    "summary": "Understand the PDF ruleset included in the core and how to adapt it.",
    "body": "# Gravewright PDF System\n\nThe **Gravewright PDF System** is part of the core and offers a basis for games whose official file is a fillable PDF.\n\n## What it provides\n\n- character type actor;\n- PDF viewer;\n- reading and writing fields;\n- mappings for token and token data;\n- integration with combat;\n- support for annotations;\n- interface in English and Portuguese.\n\n## Use in a campaign\n\n1. in **Rule Systems**, install and activate **Gravewright PDF System**;\n2. assign it to the campaign using the **Assign to campaign** selector;\n3. open the table and create an actor of the type offered by the ruleset;\n4. open the form and send a fillable PDF AcroForm;\n5. edit fields, close and reopen to confirm persistence;\n6. create a token and check the fields mapped on the board and combat.\n\n## Adapt to a game\n\nThe versioned package is in `data/packages/rulesets/gravewright-pdf-system`. Study `manifest.json`, declared schemas/layouts, `pdf-fields.gw.json`, `token.gw.json`, scripts and combat rules referenced by the manifest. For a specific game, create another ruleset ID; Do not customize the core package, as an update will overwrite your changes.\n\n## Core assets and user files\n\nThe empty A4 PDF, scripts and pdf.js runtime are part of the core and must be included in Git. User-submitted PDFs go to the campaign/`GravewrightData` storage, not the ruleset folder. Never add business listings or campaign data to the repository.\n\n## Validate\n\n```powershell\n.\\grave.bat package validate data/packages/rulesets/gravewright-pdf-system\n```\n\nThe expected result is an approved, compatible and warning-free package."
  }
];

export const guides = reviewEnglishGuides(sourceGuides);
